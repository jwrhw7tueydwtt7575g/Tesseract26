#!/bin/bash

# Log Parser & Segregator - Usage Examples

echo "╔════════════════════════════════════════════════════════════════╗"
echo "║        Log Stream Parser & Segregator - Usage Guide           ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""

echo "📚 Prerequisites:"
echo "  1. Install dependencies: pip install -r requirements.txt"
echo "  2. MongoDB cluster must be accessible"
echo ""

echo "🔧 Usage Examples:"
echo ""

echo "1️⃣  Process logs from stdin (pipe from kubectl):"
echo "   kubectl logs -n default deployment/log-generator | python main.py"
echo ""

echo "2️⃣  Process logs from a file:"
echo "   python main.py --source file --file logs.txt"
echo ""

echo "3️⃣  Process logs from Kubernetes pod:"
echo "   python main.py --source kubectl --pod log-generator-56767fd975-dr9p7 -n default"
echo ""

echo "4️⃣  Process with custom chunk size:"
echo "   python main.py --chunk-size 500 < logs.txt"
echo ""

echo "5️⃣  Process without MongoDB (local only):"
echo "   python main.py --no-mongodb --source file --file logs.txt"
echo ""

echo "📊 What the parser does:"
echo "  ✓ Reads log streams continuously"
echo "  ✓ Categorizes each log into 4 categories:"
echo "    - HEALTH (pod crashes, memory issues)"
echo "    - ANOMALY (latency, timeouts, slowness)"
echo "    - SERVICE (connectivity, HTTP errors)"
echo "    - SECURITY (auth failures, permissions)"
echo "  ✓ Extracts matching keywords for each log"
echo "  ✓ Groups logs into chunks (1000 logs per chunk)"
echo "  ✓ Stores chunks in MongoDB with statistics"
echo "  ✓ Provides aggregated statistics"
echo ""

echo "🗄️  MongoDB Schema:"
echo "  Each chunk contains:"
echo "  {
echo "    chunk_id: string,          # Unique chunk identifier"
echo "    timestamp: datetime,        # When chunk was created"
echo "    logs: {                     # Logs organized by category"
echo "      HEALTH: [...],           # Critical pod/container issues"
echo "      ANOMALY: [...],          # Performance anomalies"
echo "      SERVICE: [...],          # Service connectivity errors"
echo "      SECURITY: [...],         # Security-related errors"
echo "      IGNORED: [...],          # INFO logs (optional)"
echo "      UNKNOWN: [...]           # Uncategorized logs (optional)"
echo "    },"
echo "    stats: {                   # Statistics"
echo "      total_logs: number,"
echo "      health_count: number,"
echo "      anomaly_count: number,"
echo "      service_count: number,"
echo "      security_count: number"
echo "    }"
echo "  }"
echo ""

echo "🚀 Quick Start:"
echo "  # Install dependencies"
echo "  pip install -r requirements.txt"
echo ""
echo "  # Test with sample logs"
echo "  echo 'OOMKill - pod restart' | python main.py"
echo ""
echo "  # Process real Kubernetes logs"
echo "  kubectl logs -n default deployment/log-generator --tail=100 | python main.py"
echo ""

echo "📝 Query MongoDB:"
echo "  from pymongo import MongoClient"
echo "  client = MongoClient('mongodb+srv://...')"
echo "  db = client['k8s_logs']"
echo "  collection = db['log_streams']"
echo ""
echo "  # Get all health issues"
echo "  collection.find({'logs.HEALTH': {'\$exists': True, '\$ne': []}})"
echo ""
echo "  # Get chunks by category"
echo "  collection.find({'stats.security_count': {'\$gt': 0}})"
echo ""
echo "  # Aggregate statistics"
echo "  collection.aggregate([{'$group': {'_id': None, 'total': {'\$sum': '\$stats.total_logs'}}}])"
echo ""

echo "✅ Setup Complete!"
echo "════════════════════════════════════════════════════════════════"
